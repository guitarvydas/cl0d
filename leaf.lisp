(defun Leaf/new (given-name f)
  (let ((name (format nil "[Leaf ~a]" given-name)))
    (let ((eh (Eh/new name)))
      `((%debug . Leaf)
        (handle . ,(lambda (msg) (funcall f msg)))
	(%else . ,eh)
	))))
